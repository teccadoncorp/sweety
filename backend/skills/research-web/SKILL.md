# Web Research

Use `search_web` before you invent market facts, competitor claims, or trend language.

## How

1. Write a precise query (audience + category + time window).
2. Call `search_web`.
3. Save a short artifact with source URLs. Do not cite pages you did not actually receive.
4. If search fails, say so and work from brand context only.

services:
  web:
    build:
      context: ./frontend
    command: npm run dev
    environment:
      API_INTERNAL_URL: http://api:8000
      NEXT_PUBLIC_APP_NAME: ${APP_NAME:-Sweety}
      NODE_ENV: development
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - web_node_modules:/app/node_modules

volumes:
  web_node_modules:
